# Spawning Layer
- Owns wave spawn execution and spawn timing.
- Wave composition should come from `data/waves`, not hardcoded script tables.
- Preserve clear compatibility between runtime spawner expectations and wave config schema.
