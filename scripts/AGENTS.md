# Scripts Layer
- `scripts/` owns runtime behavior.
- Follow the nearest child `AGENTS.md` before editing a subsystem.
- Do not create new top-level runtime folders lightly; fit work into existing subsystems when possible.
- Keep gameplay rules data-backed where practical instead of duplicating tables in multiple scripts.
