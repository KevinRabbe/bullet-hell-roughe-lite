# Data Layer
- Content belongs in `data/` instead of hardcoded gameplay lists whenever possible.
- Script changes should prefer consuming structured data rather than introducing new embedded tables.
- Keep schemas stable and add compatibility guards only when migration is necessary.
