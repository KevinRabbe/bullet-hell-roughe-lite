# Wave Data
- Owns wave composition and unlock progression schema.
- Keep this data compatible with `scripts/spawning/enemy_spawner.gd`.
- Changes here should preserve deterministic wave selection and clear fallback behavior.
